import React from "react";

import "./Header.scss";
import { Logo } from "../Shared/Logo/Logo";

export const Header = () => {
  return (
    <div className="header">
      <Logo />
    </div>
  );
};
